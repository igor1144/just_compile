name: Сборка TWRP для HyperOS 2.0 (Прямой метод)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Подготовка окружения
      uses: actions/checkout@v4

    - name: Установка необходимых пакетов
      run: |
        sudo apt-get update || true
        sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf lib32readline-dev lib32z1-dev liblz4-tool libncurses6 libncurses-dev libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev unzip

    - name: Прямое скачивание готовой базы исходников TWRP
      run: |
        # Скачиваем чистое скомпонованное ядро сборщика TWRP 14.1/15 одной командой без утилиты repo
        git clone --depth=1 https://github.com -b twrp-14.1 twrp
        
    - name: Распаковка твоего дерева устройств из sea.zip
      run: |
        # Готовим папки внутри скачанной базы
        mkdir -p twrp/device/xiaomi/sea
        # Распаковываем твой архив, который лежит в корне репозитория
        unzip -o sea.zip -d /tmp/
        # Переносим твои вылизанные Lineage-файлы на свои места
        cp -r /tmp/sea/* twrp/device/xiaomi/sea/

    - name: Запуск компиляции vendor_boot
      run: |
        cd twrp
        export ALLOW_MISSING_DEPENDENCIES=true
        . build/envsetup.sh
        lunch lineage_sea-eng
        make vendorbootimage -j$(nproc --all)

    - name: Загрузка готового образа в GitHub Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: TWRP-Vendor-Boot-Sea
        path: twrp/out/target/product/sea/vendor_boot.img
